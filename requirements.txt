# ---------------------------------------------------------------
# SSD Failure Prediction under Manufacturer Distribution Shift
# ---------------------------------------------------------------
# Phase 1 — ingest, verification, profiling
#   src/inspect_dataset.py
#   src/preprocess_data.py
#   label2.py
# ---------------------------------------------------------------

pandas>=2.1,<3.0
pyarrow>=15.0
polars>=1.0
numpy>=1.26
pytest>=8.0
scikit-learn>=1.4
torch>=2.2
# ---------------------------------------------------------------
# Later phases — uncomment as you reach them.
# Kept out of the default install so Phase 1 stays a light setup
# and CI/Colab bootstrap remains fast.
# ---------------------------------------------------------------

# Step 2 — WEFR baseline (ensemble ranking, Random Forest)
# scikit-learn>=1.4
# scipy>=1.11

# Step 3 — temporal sequence model
# torch>=2.2

# Steps 5-6 — figures and reporting
# matplotlib>=3.8

# Optional — interactive results viewer
# streamlit>=1.30

# Optional — PDF deliverable generation
# reportlab>=4.0
